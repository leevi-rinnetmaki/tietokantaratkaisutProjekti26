spring:
  application:
    name: demo
  datasource:
    url: jdbc:mariadb://localhost:3306/population26
    username: demouser
    password: demouser
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
